# OpenQC Agent Context

OpenQC consumes `gaussian-lsp-tool` and `lsp-capabilities.json` to assemble diagnostics, hover, completion, symbols, examples, next-token guidance, and repair-plan hints for `gaussian` documents.
